/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package de.clashsoft.gradle.angular;

import org.gradle.testfixtures.ProjectBuilder;
import org.gradle.api.Project;
import org.junit.Test;
import static org.junit.Assert.*;

/**
 * A simple unit test for the 'de.clashsoft.gradle.angular.greeting' plugin.
 */
public class AngularGradlePluginTest
{
    @Test public void pluginRegistersATask() {
        // Create a test project and apply the plugin
        Project project = ProjectBuilder.builder().build();
        project.getPlugins().apply("de.clashsoft.gradle.angular.greeting");

        // Verify the result
        assertNotNull(project.getTasks().findByName("greeting"));
    }
}
